spring:
  application:
    name: backend
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8080

attendance:
  scan:
    timezone: Asia/Phnom_Penh

app:
  password-reset:
    otp-expiry-minutes: 10
    mail-from: no-reply@general-affair.local
